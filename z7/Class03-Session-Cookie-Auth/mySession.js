const express = require('express');

const app = express();
const PORT = 3000;

app.get('./', (res,req)=>{

})

app.post('./', (res,req)=>{
    
})
app.listen(PORT, (res,req)=>{
    console.log(`server started on port ${PORT}`);
})